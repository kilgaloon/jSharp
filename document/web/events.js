

plug.onabort = function( foo ) {
	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onabort = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onabort = foo;
		}

	}
}

plug.onbeforeupload = function( foo ) {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onbeforeupload = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onbeforeupload = foo;
		}

	}

}

plug.onblur = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onblur = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onblur = foo;
		}

	}

}

plug.oncancel = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.oncancel = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].oncancel = foo;
		}

	}

}

plug.oncanplay = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.oncanplay = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].oncanplay = foo;
		}

	}

}

plug.oncanplaythrough = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.oncanplaythrough = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].oncanplaythrough = foo;
		}

	}

}

plug.onchange = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onchange = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onchange = foo;
		}

	}

}

plug.onclick = function( foo ) {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onclick = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onclick = foo;
		}

	}
}

plug.onclose = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onclose = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onclose = foo;
		}

	}

}

plug.onrclick = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.oncontextmenu = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].oncontextmenu = foo;
		}

	}

}

plug.oncuechange = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.oncuechange = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].oncuechange = foo;
		}

	}

}

plug.ondblclick = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.ondblclick = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].ondblclick = foo;
		}

	}

}

plug.ondevicemotion = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.ondevicemotion = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].ondevicemotion = foo;
		}

	}

}

plug.ondeviceoriantation = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.ondeviceoriantation = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].ondeviceoriantation = foo;
		}

	}

}

plug.ondrag = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.ondrag = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].ondrag = foo;
		}

	}

}

plug.ondragend = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.ondragend = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].ondragend = foo;
		}

	}

}

plug.ondragenter = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.ondragenter = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].ondragenter = foo;
		}

	}

}

plug.ondragleave = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.ondragleave = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].ondragleave = foo;
		}

	}

}

plug.ondragover = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.ondragover = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].ondragover = foo;
		}

	}

}

plug.ondragstart = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.ondragstart = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].ondragstart = foo;
		}

	}

}

plug.ondrop = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.ondrop = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].ondrop = foo;
		}

	}

}

plug.ondurationchange = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.ondurationchange = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].ondurationchange = foo;
		}

	}

}

plug.onemptied = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onemptied = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onemptied = foo;
		}

	}

}

plug.onended = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onended = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onended = foo;
		}

	}

}

plug.onerror = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onerror = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onerror = foo;
		}

	}

}

plug.onfocus = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onfocus = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onfocus = foo;
		}

	}

}

plug.onhashchange = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onhashchange = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onhashchange = foo;
		}

	}

}

plug.oninput = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.oninput = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].oninput = foo;
		}

	}

}

plug.oninvalid = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.oninvalid = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].oninvalid = foo;
		}

	}

}

plug.onkeydown = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onkeydown = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onkeydown = foo;
		}

	}

}

plug.onkeypress = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onkeypress = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onkeypress = foo;
		}

	}

}

plug.onkeyup = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onkeyup = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onkeyup = foo;
		}

	}

}

plug.onload = function( foo ) {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onload = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onload = foo;
		}

	}

}

plug.onloadeddata = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onloadeddata = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onloadeddata = foo;
		}

	}

}

plug.onloadedmetadata = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onloadedmetadata = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onloadedmetadata = foo;
		}

	}

}

plug.onloadstart = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onloadstart = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onloadstart = foo;
		}

	}

}

plug.onmessage = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onmessage = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onmessage = foo;
		}

	}

}

plug.onmousedown = function( foo ) {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onmousedown = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onmousedown = foo;
		}

	}

}

plug.onmouseenter = function( foo ) {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onmouseenter = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onmouseenter = foo;
		}

	}

}

plug.onmouseleave = function( foo ) {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onmouseleave = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onmouseleave = foo;
		}

	}

}

plug.onmousemove = function( foo ) {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onmousemove = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onmousemove = foo;
		}

	}

}

plug.onmouseout = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onmouseout = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onmouseout = foo;
		}

	}

}

plug.onhover = function( foo ) {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onmouseover = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onmouseover = foo;
		}

	}

}

plug.onmouseup = function( foo ) {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onmouseup = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onmouseup = foo;
		}

	}

}

plug.onmousewheel = function( foo ) {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onmousewheel = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onmousewheel = foo;
		}

	}

}

plug.onwheelup = function( foo ) {

	if(typeof object_in_buffer.length == "undefined") {

		object_in_buffer.onmousewheel = function( foo )  {

			if(event.wheelDelta < 0) {
				foo();
			}

		};

	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onmousewheel = function( foo )  {

				if(event.wheelDelta < 0) {
					foo();
				}

			};
		}

	}

}

plug.onwheeldown = function( foo ) {

	if(typeof object_in_buffer.length == "undefined") {

		object_in_buffer.onmousewheel = function( foo )  {

			if(event.wheelDelta > 0) {
				foo();
			}

		};

	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onmousewheel = function( foo )  {

				if(event.wheelDelta > 0) {
					foo();
				}

			};
		}

	}

}

plug.onoffline = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onoffline = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onoffline = foo;
		}

	}

}

plug.ononline = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.ononline = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].ononline = foo;
		}

	}

}

plug.onpagehide = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onpagehide = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onpagehide = foo;
		}

	}

}

plug.onpageshow = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onpageshow = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onpageshow = foo;
		}

	}

}

plug.onpause = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onpause = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onpause = foo;
		}

	}

}

plug.onplay = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onplay = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onplay = foo;
		}

	}

}

plug.onplaying = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onplaying = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onplaying = foo;
		}

	}

}

plug.onpopstate = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onpopstate = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onpopstate = foo;
		}

	}

}

plug.onprogress = function( foo ) {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onprogress = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onprogress = foo;
		}

	}

}

plug.onratechange = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onratechange = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onratechange = foo;
		}

	}

}

plug.onreset = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onreset = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onreset = foo;
		}

	}

}

plug.onresize = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onresize = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onresize = foo;
		}

	}

}

plug.onscroll = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onscroll = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onscroll = foo;
		}

	}

}

plug.onsearch = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onsearch = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onsearch = foo;
		}

	}

}

plug.onseeked = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onseeked = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onseeked = foo;
		}

	}

}

plug.onseeking = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onseeking = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onseeking = foo;
		}

	}

}

plug.onselect = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onselect = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onselect = foo;
		}

	}

}

plug.onshow = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onshow = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onshow = foo;
		}

	}

}

plug.onstalled = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onstalled = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onstalled = foo;
		}

	}

}

plug.onstorage = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onstorage = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onstorage = foo;
		}

	}

}

plug.onsubmit = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onsubmit = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onsubmit = foo;
		}

	}

}

plug.onsuspend = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onsuspend = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onsuspend = foo;
		}

	}

}

plug.ontimeupdate = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.ontimeupdate = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].ontimeupdate = foo;
		}

	}

}

plug.ontransitionend = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.ontransitionend = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].ontransitionend = foo;
		}

	}

}

plug.onunload = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onunload = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onunload = foo;
		}

	}

}

plug.onvolumechange = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onvolumechange = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onvolumechange = foo;
		}

	}

}

plug.onwaiting = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onwaiting = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onwaiting = foo;
		}

	}

}

plug.onwheel = function( foo )  {

	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.onwheel = foo;
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].onwheel = foo;
		}

	}

}

plug.listen = function( listener, foo ) {	
	if(typeof object_in_buffer.length == "undefined") {
		object_in_buffer.addEventListener(listener, foo);
	} else {

		for(i = 0; i < object_in_buffer.length; i++) {
			object_in_buffer[i].addEventListener(listener, foo);
		}

	}
}


plug.swipe = function( side, distance, foo) {
	object_in_buffer.addEventListener('touchstart', function( event ) {
		touchStartedCoordinate = event.changedTouches[0].pageX;
	});

	object_in_buffer.addEventListener('touchend', function( event ) {
		touchEndedCoordinate = event.changedTouches[0].pageX;
		calculateDistance = touchStartedCoordinate - touchEndedCoordinate;

		
		if( calculateDistance < 0 && calculateDistance < -distance ) {
			if(side === 'right') {
				foo();
			}
		} else if( distance < calculateDistance ){
			if(side === 'left') {
				foo();
			}
		}

	});

}
